version: 0.0
os: linux
files:
        - source: /
          destination: /home/ubuntu/nodeapp
hooks:
        AfterInstall:
                - location: AfterInstallHook.sh
                  timeout: 300
